{
  "name": "time-tracker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "cypress:open": "cypress open",
    "deploy": "pnpm build --base=/time-tracker && gh-pages -d dist",
    "prepare": "husky install"
  },
  "dependencies": {
    "preact": "^10.16.0",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "@types/mocha": "^10.0.1",
    "cypress": "^12.17.3",
    "gh-pages": "^5.0.0",
    "husky": "^8.0.3",
    "sass": "^1.64.2",
    "typescript": "^5.1.6",
    "vite": "^4.4.5"
  }
}